{% extends "base.html" %}

{% block head %}
    <link rel="stylesheet" href="{{ url_for('static', filename='css/admin_payment_selection.css') }}">
{% endblock %}

{% block content %}
<div class="container text-center content"> <!-- Add the content class for margin adjustment -->
    <!-- Back Button to go back to the menu -->
    <div class="text-left">
        <a href="{{ url_for('menu') }}" class="btn btn-secondary mb-3" onclick="window.location.reload();">Back to Menu</a>
    </div>

    <!-- Total Amount Header -->
    <h1>Total Amount: RM<span id="total-amount">{{ total_amount }}</span></h1>

    <!-- Two-column layout -->
    <div class="row">
        <!-- Left Column: Order List -->
        <div class="col-md-8" style="padding-left: 100px;">
            <div class="order-list">
                <h2>Order List</h2>
                <ul id="order-items">
                    {% for item in order_items %}
                    <li class="d-flex justify-content-between align-items-center">
                        <div>{{ item.quantity }}x {{ item.name }}</div>
                        <div>RM{{ item.total_price }}</div>
                    </li>
                    {% endfor %}
                </ul>
                
                <div class="order-summary">
                    <p><strong>Total:</strong> RM<span id="total-price">{{ total_amount }}</span></p>
                </div>
            </div>
        </div>

        <!-- Right Column: Payment Options -->
        <div class="col-md-4">
            <div class="payment-options" style="border: 1px solid #ddd; padding: 15px; border-radius: 5px;">
                <h3>Select Payment Method</h3>
                <div class="row">
                    {% for payment_type in payment_types %}
                    <div class="col-md-12 mb-3">
                        <button class="btn btn-primary btn-lg btn-block payment-button" data-payment-type="{{ payment_type }}">{{ payment_type }}</button>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    $(document).ready(function() {
        // Handle payment button click
        $('.payment-button').click(function() {
            let paymentType = $(this).data('payment-type');
            
            // Redirect to the payment processing page with the selected payment type
            window.location.href = "{{ url_for('payment_success') }}?payment_type=" + encodeURIComponent(paymentType) + "&total_amount=" + encodeURIComponent('{{ total_amount }}');
        });
    });
</script>

{% endblock %}


















/* General Styles */
body {
    font-family: Arial, sans-serif;
    background-color: #f8f9fa;
    margin: 0;
    padding: 0;
}

.content {
    margin-left: 200px;
    padding: 20px;
    transition: margin-left 0.3s;
}

.content.expanded {
    margin-left: 50px;
}

.container {
    max-width: 900px;
    margin: 40px auto;
    padding: 20px;
    background-color: #fff;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

/* Back Button */
.container .text-left a {
    color: white;
    text-decoration: none;
}

.container .text-left .btn-secondary {
    background-color: #6c757d;
    border-color: #6c757d;
}

/* Total Amount Section */
h1 {
    font-size: 36px;
    margin-bottom: 30px;
    color: #333;
}

/* Order List */
.order-list {
    width: 100%; /* Make sure it fits within the content area */
    border: 1px solid #ddd;
    padding: 15px;
    border-radius: 5px;
    background-color: #fff; /* Add background for better visibility */
    box-sizing: border-box; /* Ensure padding doesn't affect width */
}

.order-list h2 {
    font-size: 24px;
    margin-bottom: 15px;
}

#order-items {
    list-style-type: none;
    padding: 0;
}

#order-items li {
    padding: 10px;
    border-bottom: 1px solid #ddd;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

#order-items li:last-child {
    border-bottom: none;
}

.order-summary {
    margin-top: 20px;
}

.order-summary p {
    font-size: 18px;
    font-weight: bold;
}

/* Payment Options */
.payment-options {
    border: 1px solid #ddd;
    padding: 15px;
    border-radius: 5px;
    background-color: #f5f5f5;
}

.payment-options h3 {
    font-size: 22px;
    margin-bottom: 20px;
}

.payment-button {
    width: 100%;
    padding: 15px;
    font-size: 18px;
    border-radius: 5px;
    cursor: pointer;
    background-color: #007bff;
    color: white;
    border: none;
}

.payment-button:hover {
    background-color: #0056b3;
}

.payment-button:focus {
    outline: none;
}
